---
description: Start all development services (Docker, backend, frontend)
---

Start the complete development environment:

1. Check if Docker services are running (postgres, redis, localstack)
2. If not running, start them with docker-compose up -d
3. Check backend status and provide instructions to start if needed
4. Check frontend status and provide instructions to start if needed
5. Display all service URLs and status
